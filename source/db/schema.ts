import { sqliteTable, text, integer, uniqueIndex, index, check } from 'drizzle-orm/sqlite-core';
import { sql } from 'drizzle-orm';
export const products = sqliteTable('products', { id: text().primaryKey(), name: text().notNull(), category: text().notNull(), price: integer().notNull(), oldPrice: integer('old_price').notNull().default(0), description: text().notNull(), material: text().notNull(), fit: text().notNull(), wash: text().notNull(), images: text().notNull(), active: integer().notNull().default(1), createdAt: integer('created_at').notNull() }, t => [check('price_positive', sql `${t.price}>0`), index('idx_products_category').on(t.category)]);
export const categories = sqliteTable('categories', { name: text().primaryKey() });
export const variants = sqliteTable('variants', { id: text().primaryKey(), productId: text('product_id').notNull().references(() => products.id), color: text().notNull(), size: text().notNull(), stock: integer().notNull() }, t => [check('stock_nonnegative', sql `${t.stock}>=0`), uniqueIndex('idx_variants_options').on(t.productId, t.color, t.size)]);
export const users = sqliteTable('users', { id: text().primaryKey(), email: text().notNull(), name: text().notNull() });
export const admin = sqliteTable('admin', { id: integer().primaryKey(), userId: text('user_id').notNull().references(() => users.id) });
export const settings = sqliteTable('settings', { key: text().primaryKey(), value: text().notNull() });
export const cart = sqliteTable('cart', { id: text().primaryKey(), owner: text().notNull(), variantId: text('variant_id').notNull().references(() => variants.id), quantity: integer().notNull() }, t => [uniqueIndex('idx_cart_owner_variant').on(t.owner, t.variantId), check('cart_quantity_positive', sql `${t.quantity}>0`)]);
export const orders = sqliteTable('orders', { id: text().primaryKey(), owner: text().notNull(), idempotency: text().notNull().unique(), name: text().notNull(), phone: text().notNull(), address: text().notNull(), postal: text().notNull(), shipping: text().notNull(), shippingCost: integer('shipping_cost').notNull(), payment: text().notNull(), paymentStatus: text('payment_status').notNull().default('pending'), status: text().notNull().default('جدید'), total: integer().notNull(), createdAt: integer('created_at').notNull(), tracking: text().notNull().default('') }, t => [index('idx_orders_owner').on(t.owner), index('idx_orders_created').on(t.createdAt)]);
export const orderItems = sqliteTable('order_items', { id: text().primaryKey(), orderId: text('order_id').notNull().references(() => orders.id), variantId: text('variant_id').notNull().references(() => variants.id), name: text().notNull(), image: text().notNull(), size: text().notNull(), color: text().notNull(), price: integer().notNull(), quantity: integer().notNull() }, t => [index('idx_order_items_order').on(t.orderId), check('item_quantity_positive', sql `${t.quantity}>0`)]);
export const payments = sqliteTable('payments', { id: text().primaryKey(), orderId: text('order_id').notNull().references(() => orders.id), provider: text().notNull(), status: text().notNull(), amount: integer().notNull(), createdAt: integer('created_at').notNull() });
export const inventory = sqliteTable('inventory', { id: text().primaryKey(), variantId: text('variant_id').notNull(), delta: integer().notNull(), reason: text().notNull(), createdAt: integer('created_at').notNull() });
export const coupons = sqliteTable('coupons', { code: text().primaryKey(), percent: integer().notNull(), active: integer().notNull().default(0) });
export const rateLimits = sqliteTable('rate_limits', { key: text().primaryKey(), count: integer().notNull(), until: integer().notNull() });
